import { type ComponentProps } from "solid-js"

export const Mark = (_props: { class?: string }) => {
  return <></>
}

export const Splash = (_props: Pick<ComponentProps<"svg">, "ref" | "class">) => {
  return <></>
}

export const Logo = (_props: { class?: string }) => {
  return <></>
}
